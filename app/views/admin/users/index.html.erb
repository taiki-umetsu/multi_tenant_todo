<div class="max-w-6xl mx-auto p-6">
  <div class="bg-white rounded-lg shadow-md p-6 space-y-8">
    <!-- ページタイトル -->
    <div>
      <h1 class="text-3xl font-bold text-gray-900 mb-2">ユーザー管理</h1>
      <p class="text-gray-600">テナント内のユーザーの管理と招待を行います</p>
    </div>

    <!-- ユーザー招待機能 -->
    <div>
      <h2 class="text-xl font-semibold text-gray-900 mb-4">ユーザー招待</h2>
      <p class="text-gray-500">招待機能は実装中です</p>
    </div>

    <!-- 招待中ユーザー表示 -->
    <div>
      <h2 class="text-xl font-semibold text-gray-900 mb-4">招待中のユーザー</h2>
      <p class="text-gray-500">招待状況の表示機能は実装中です</p>
    </div>

    <!-- ユーザー一覧表示 -->
    <div>
      <div class="flex items-center justify-between mb-4">
        <h2 class="text-xl font-semibold text-gray-900">登録済みユーザー</h2>
        <div class="text-sm text-gray-500">
          <%= @users.count %>名のユーザー
        </div>
      </div>

      <% if @users.any? %>
        <div class="overflow-x-auto">
          <table class="w-full max-w-4xl divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/2">
                  メールアドレス
                </th>
                <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/4">
                  権限
                </th>
                <th class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider w-1/4">
                  登録日
                </th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
              <% @users.each do |user| %>
                <tr class="hover:bg-gray-50">
                  <td class="px-4 py-4 whitespace-nowrap">
                    <div class="text-sm text-gray-900">
                      <%= user.email %>
                    </div>
                  </td>
                  <td class="px-4 py-4 whitespace-nowrap">
                    <% if user.admin? %>
                      <span class="inline-flex px-2 py-1 text-xs rounded-full bg-green-100 text-green-800">
                        管理者
                      </span>
                    <% else %>
                      <span class="inline-flex px-2 py-1 text-xs rounded-full bg-blue-100 text-blue-800">
                        メンバー
                      </span>
                    <% end %>
                  </td>
                  <td class="px-4 py-4 whitespace-nowrap text-sm text-gray-500">
                    <%= l(user.created_at.to_date, format: :long) %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="text-center py-8 text-gray-500">
          <p>登録済みユーザーはいません</p>
        </div>
      <% end %>
    </div>
  </div>
</div>